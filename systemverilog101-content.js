const course = {
  "shortTitle": "SystemVerilog 101",
  "title": "Introduction to SystemVerilog",
  "subtitle": "SystemVerilog fundamentals for commercial RTL design",
  "objective": "",
  "objectiveHtml": "<p>By the end of this lecture, learners will be able to:</p>\n<ul><li>Explain why SystemVerilog was developed.</li><li>Identify the relationship between Verilog and SystemVerilog.</li><li>Distinguish between SystemVerilog design and verification features.</li><li>Select the correct file extension for Verilog and SystemVerilog source files.</li><li>Recognize where SystemVerilog is used in the commercial RTL workflow.</li><li>Review AI-generated RTL and identify whether it uses appropriate SystemVerilog constructs.</li></ul>",
  "sections": [
    {
      "id": "introduction",
      "title": "Introduction",
      "topics": [],
      "contentHtml": "<h3>What Is SystemVerilog?</h3>\n<ul><li>SystemVerilog is a hardware design and verification language.</li><li>It extends the capabilities originally provided by Verilog.</li><li>It supports:<ul><li>RTL design</li><li>Gate-level modeling</li><li>Testbench development</li><li>Assertions</li><li>Functional coverage</li><li>Object-oriented verification</li><li>Constrained-random verification</li></ul></li><li>SystemVerilog is standardized under IEEE 1800.</li><li>It provides one language for both:<ul><li>Hardware implementation</li><li>Hardware verification</li></ul></li></ul>\n<h3>SystemVerilog in the RTL Workflow</h3>\n<pre><code class=\"language-text\">Design Specification\n        ↓\nSystemVerilog RTL\n        ↓\nSystemVerilog Testbench\n        ↓\nSimulation\n        ↓\nWaveform Analysis\n        ↓\nDebugging\n        ↓\nSynthesis</code></pre>\n<h3>Why Should an RTL Engineer Learn SystemVerilog?</h3>\n<ul><li>Modern RTL projects commonly use SystemVerilog instead of only Verilog.</li><li>SystemVerilog provides clearer constructs for describing hardware.</li><li>It reduces ambiguity between:<ul><li>Combinational logic</li><li>Sequential logic</li><li>Intentional latch logic</li></ul></li><li>It improves the readability of:<ul><li>State machines</li><li>Control logic</li><li>Interfaces</li><li>Testbenches</li></ul></li><li>It helps simulation, linting and synthesis tools detect certain coding mistakes.</li><li>It supports both simple RTL blocks and large ASIC or FPGA projects.</li></ul>\n<h3>Why Learn It in the AI Era?</h3>\n<p>AI tools can generate SystemVerilog, but generated code must still be reviewed by an engineer.</p>\n<p>The engineer must verify:</p>\n<ul><li>Whether the code is synthesizable.</li><li>Whether combinational logic is complete.</li><li>Whether sequential logic uses the correct clock and reset.</li><li>Whether an unintended latch has been created.</li><li>Whether signals have multiple drivers.</li><li>Whether the code matches the design specification.</li><li>Whether the simulator and synthesis tool support the generated constructs.</li></ul>\n<h3>Example: AI-Generated RTL Must Be Reviewed</h3>\n<pre><code class=\"language-systemverilog\">module and_gate (\n    input  logic a,\n    input  logic b,\n    output logic y\n);\n\n    always_comb begin\n        y = a &amp; b;\n    end\n\nendmodule</code></pre>\n<p>Engineering review questions:</p>\n<ul><li>Is <code>y</code> assigned for every possible input condition?</li><li>Is <code>always_comb</code> suitable for this logic?</li><li>Does the module infer only combinational hardware?</li><li>Could a continuous assignment provide a simpler implementation?</li></ul>\n<p>An alternative implementation is:</p>\n<pre><code class=\"language-systemverilog\">module and_gate (\n    input  logic a,\n    input  logic b,\n    output logic y\n);\n\n    assign y = a &amp; b;\n\nendmodule</code></pre>\n<p>Both descriptions represent combinational logic.</p>\n<p>The engineer selects the style that communicates the design intent clearly.</p>\n<h3>Lecture Scope</h3>\n<p>This lecture focuses on SystemVerilog features commonly required for introductory RTL design:</p>\n<ul><li><code>.sv</code> source files</li><li><code>logic</code></li><li><code>always_comb</code></li><li><code>always_ff</code></li><li><code>always_latch</code></li><li><code>typedef</code></li><li><code>enum</code></li></ul>\n<p>The following features will only be introduced briefly:</p>\n<ul><li>Interfaces</li><li>Assertions</li><li>Classes</li><li>Randomization</li><li>UVM</li></ul>\n<p>These advanced features will be studied later when they become necessary in the engineering workflow.</p>"
    },
    {
      "id": "why-was-systemverilog-created",
      "title": "Why Was SystemVerilog Created?",
      "topics": [],
      "contentHtml": "<h3>Limitations of Traditional Verilog</h3>\n<p>Verilog is capable of describing synthesizable digital hardware, but large commercial projects exposed several limitations.</p>\n<p>Common limitations include:</p>\n<ul><li>The same <code>always</code> construct can describe different types of hardware.</li><li>Designers must manually maintain sensitivity lists in older coding styles.</li><li>The distinction between <code>wire</code> and <code>reg</code> can confuse beginners.</li><li>State values are often represented using manually defined parameters.</li><li>Large testbenches are difficult to organize using only traditional Verilog constructs.</li><li>Verilog does not provide the complete verification capabilities required by modern ASIC projects.</li></ul>\n<h3>Example: Generic Verilog <code>always</code> Block</h3>\n<pre><code class=\"language-verilog\">reg y;\n\nalways @(a or b or select) begin\n    if (select)\n        y = b;\n    else\n        y = a;\nend</code></pre>\n<p>Problems that may occur:</p>\n<ul><li>A signal can accidentally be omitted from the sensitivity list.</li><li>The keyword <code>reg</code> does not indicate that a physical register will be created.</li><li>The <code>always</code> keyword does not clearly communicate whether the intended hardware is:<ul><li>Combinational logic</li><li>Flip-flop logic</li><li>Latch logic</li></ul></li></ul>\n<h3>SystemVerilog Improvement</h3>\n<pre><code class=\"language-systemverilog\">logic y;\n\nalways_comb begin\n    if (select)\n        y = b;\n    else\n        y = a;\nend</code></pre>\n<p>Advantages:</p>\n<ul><li><code>always_comb</code> communicates combinational design intent.</li><li>The simulator determines the sensitivity automatically.</li><li>Tools can perform additional checks on the block.</li><li><code>logic</code> provides a clearer variable declaration for many RTL signals.</li></ul>\n<h3>Increasing ASIC and FPGA Complexity</h3>\n<p>Modern digital systems contain:</p>\n<ul><li>Multiple clocks</li><li>Multiple reset domains</li><li>Complex state machines</li><li>Reusable IP blocks</li><li>Standard bus interfaces</li><li>Large verification environments</li><li>Assertions and coverage models</li><li>Hundreds or thousands of interconnected modules</li></ul>\n<p>A larger project requires:</p>\n<ul><li>Stronger data types</li><li>Clearer design intent</li><li>Better code reuse</li><li>Better verification support</li><li>Improved tool checking</li><li>Consistent design and verification terminology</li></ul>\n<h3>One Language for Design and Verification</h3>\n<p>SystemVerilog combines features for two major engineering activities.</p>\n<h4>Design Features</h4>\n<ul><li>Modules</li><li>Interfaces</li><li><code>logic</code></li><li><code>always_comb</code></li><li><code>always_ff</code></li><li><code>always_latch</code></li><li><code>typedef</code></li><li><code>enum</code></li><li>Structures</li><li>Packages</li><li>Parameterized RTL</li></ul>\n<h4>Verification Features</h4>\n<ul><li>Assertions</li><li>Classes</li><li>Constrained randomization</li><li>Functional coverage</li><li>Mailboxes</li><li>Semaphores</li><li>Clocking blocks</li><li>UVM-based environments</li></ul>\n<p>This allows design and verification teams to use a common language while working with different language subsets.</p>\n<h3>IEEE Standardization</h3>\n<ul><li>SystemVerilog is defined by IEEE Standard 1800.</li><li>The standard specifies:<ul><li>Language syntax</li><li>Language semantics</li><li>Design constructs</li><li>Verification constructs</li><li>Simulation behavior</li><li>Programming interfaces</li></ul></li><li>Commercial and open-source tools implement supported portions of this standard.</li><li>Tool support must still be checked before selecting advanced language features.</li></ul>\n<h3>Engineering Significance</h3>\n<p>SystemVerilog was not created merely to shorten Verilog code.</p>\n<p>It was created to support:</p>\n<ul><li>More precise hardware modeling</li><li>Scalable RTL development</li><li>Reusable design components</li><li>Structured verification</li><li>Earlier detection of coding errors</li><li>Communication of design intent to tools and engineers</li></ul>"
    },
    {
      "id": "verilog-vs-systemverilog",
      "title": "Verilog vs SystemVerilog",
      "topics": [],
      "contentHtml": "<h3>Relationship Between the Languages</h3>\n<ul><li>SystemVerilog extends Verilog.</li><li>Most synthesizable Verilog code can be compiled as SystemVerilog.</li><li>Existing Verilog modules can normally be reused in SystemVerilog projects.</li><li>SystemVerilog introduces additional:<ul><li>Keywords</li><li>Data types</li><li>Procedural blocks</li><li>Verification capabilities</li></ul></li><li>Existing identifiers that match new SystemVerilog reserved keywords may require modification.</li></ul>\n<h3>Basic Verilog Module</h3>\n<pre><code class=\"language-verilog\">module mux2 (\n    input  wire a,\n    input  wire b,\n    input  wire select,\n    output reg  y\n);\n\n    always @(*) begin\n        if (select)\n            y = b;\n        else\n            y = a;\n    end\n\nendmodule</code></pre>\n<h3>Equivalent SystemVerilog Module</h3>\n<pre><code class=\"language-systemverilog\">module mux2 (\n    input  logic a,\n    input  logic b,\n    input  logic select,\n    output logic y\n);\n\n    always_comb begin\n        if (select)\n            y = b;\n        else\n            y = a;\n    end\n\nendmodule</code></pre>\n<h3>Key Differences</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Area</th><th>Verilog</th><th>SystemVerilog</th></tr></thead><tbody><tr><td>Primary standard</td><td>IEEE 1364</td><td>IEEE 1800</td></tr><tr><td>Common source extension</td><td><code>.v</code></td><td><code>.sv</code></td></tr><tr><td>General RTL variable</td><td><code>reg</code></td><td><code>logic</code></td></tr><tr><td>Combinational block</td><td><code>always @(*)</code></td><td><code>always_comb</code></td></tr><tr><td>Sequential block</td><td><code>always @(posedge clk)</code></td><td><code>always_ff @(posedge clk)</code></td></tr><tr><td>Latch block</td><td>Generic <code>always</code> block</td><td><code>always_latch</code></td></tr><tr><td>State declaration</td><td><code>parameter</code> or <code>localparam</code></td><td><code>enum</code> with <code>typedef</code></td></tr><tr><td>User-defined data types</td><td>Limited</td><td>Strong <code>typedef</code> support</td></tr><tr><td>Assertions</td><td>Not integrated</td><td>SystemVerilog Assertions</td></tr><tr><td>Object-oriented verification</td><td>Not available</td><td>Classes supported</td></tr><tr><td>Random verification</td><td>Limited manual implementation</td><td>Constrained randomization</td></tr><tr><td>UVM support</td><td>Not native</td><td>Built using SystemVerilog classes</td></tr></tbody></table></div>\n<h3>Synthesizable Features</h3>\n<p>Synthesizable SystemVerilog features describe hardware that can be mapped into gates and registers.</p>\n<p>Examples include:</p>\n<ul><li>Modules</li><li>Ports</li><li>Continuous assignments</li><li><code>logic</code></li><li><code>always_comb</code></li><li><code>always_ff</code></li><li><code>always_latch</code></li><li><code>if</code></li><li><code>case</code></li><li><code>for</code></li><li><code>typedef</code></li><li><code>enum</code></li><li>Parameters</li><li>Generate blocks</li></ul>\n<p>Example:</p>\n<pre><code class=\"language-systemverilog\">module register_8bit (\n    input  logic       clk,\n    input  logic       reset_n,\n    input  logic [7:0] data_in,\n    output logic [7:0] data_out\n);\n\n    always_ff @(posedge clk or negedge reset_n) begin\n        if (!reset_n)\n            data_out &lt;= 8&#x27;h00;\n        else\n            data_out &lt;= data_in;\n    end\n\nendmodule</code></pre>\n<p>Expected hardware:</p>\n<ul><li>Eight flip-flops</li><li>An asynchronous active-low reset</li><li>A common clock input</li></ul>\n<h3>Verification Features</h3>\n<p>Verification features are primarily used during simulation and are generally not synthesized into hardware.</p>\n<p>Examples include:</p>\n<ul><li>Classes</li><li>Randomization</li><li>Coverage groups</li><li>Mailboxes</li><li>Semaphores</li><li>Dynamic arrays</li><li>Queues</li><li>Advanced assertions</li><li>UVM components</li></ul>\n<p>Example:</p>\n<pre><code class=\"language-systemverilog\">initial begin\n    assert (data_out == 8&#x27;h00)\n        $display(&quot;Reset verification passed&quot;);\n    else\n        $error(&quot;Reset verification failed&quot;);\nend</code></pre>\n<p>This assertion checks simulation behavior.</p>\n<p>It does not describe the functional datapath of the synthesized circuit.</p>\n<h3>Why Industry Uses SystemVerilog</h3>\n<p>SystemVerilog supports the complete front-end workflow:</p>\n<pre><code class=\"language-text\">Architecture\n    ↓\nRTL Design\n    ↓\nLinting\n    ↓\nFunctional Verification\n    ↓\nAssertions and Coverage\n    ↓\nSynthesis\n    ↓\nFront-End Signoff</code></pre>\n<p>Commercial advantages include:</p>\n<ul><li>Clearer RTL intent</li><li>Better tool checking</li><li>Reusable data types</li><li>More readable state machines</li><li>Unified design and verification syntax</li><li>Support for scalable verification methodologies</li><li>Improved collaboration between RTL and verification engineers</li></ul>\n<h3>SystemVerilog Does Not Eliminate Engineering Review</h3>\n<p>The use of SystemVerilog does not guarantee correct hardware.</p>\n<p>Engineers must still verify:</p>\n<ul><li>Functional correctness</li><li>Synthesizability</li><li>Clock and reset behavior</li><li>Complete combinational assignments</li><li>Multiple-driver conditions</li><li>Timing assumptions</li><li>Interface behavior</li><li>Tool compatibility</li></ul>"
    },
    {
      "id": "where-we-will-use-systemverilog-in-this-course",
      "title": "Where We Will Use SystemVerilog in This Course",
      "topics": [],
      "contentHtml": "<h3>RTL Generation</h3>\n<p>ATI can generate SystemVerilog RTL from an engineering specification.</p>\n<p>Typical input:</p>\n<pre><code class=\"language-text\">Functional requirements\nInterface requirements\nClock and reset requirements\nTiming behavior\nParameter requirements\nCoding guidelines</code></pre>\n<p>Typical output:</p>\n<pre><code class=\"language-text\">Synthesizable .sv module\nSupporting package\nReusable parameters and types\nImplementation notes</code></pre>\n<p>Example SystemVerilog RTL:</p>\n<pre><code class=\"language-systemverilog\">module up_counter #(\n    parameter int WIDTH = 8\n) (\n    input  logic             clk,\n    input  logic             reset_n,\n    input  logic             enable,\n    output logic [WIDTH-1:0] count\n);\n\n    always_ff @(posedge clk or negedge reset_n) begin\n        if (!reset_n)\n            count &lt;= &#x27;0;\n        else if (enable)\n            count &lt;= count + 1&#x27;b1;\n    end\n\nendmodule</code></pre>\n<p>Review points:</p>\n<ul><li><code>WIDTH</code> makes the module reusable.</li><li><code>logic</code> is used for the ports.</li><li><code>always_ff</code> communicates sequential intent.</li><li>Nonblocking assignment is used for the register.</li><li><code>'0</code> automatically matches the width of <code>count</code>.</li></ul>\n<h3>Testbench Development</h3>\n<p>SystemVerilog can be used to create testbenches that:</p>\n<ul><li>Generate clocks and resets.</li><li>Apply input stimulus.</li><li>Monitor outputs.</li><li>Compare actual and expected values.</li><li>Report pass or fail results.</li><li>Add assertions and coverage later.</li></ul>\n<p>Example:</p>\n<pre><code class=\"language-systemverilog\">module up_counter_tb;\n\n    logic       clk;\n    logic       reset_n;\n    logic       enable;\n    logic [7:0] count;\n\n    up_counter #(\n        .WIDTH(8)\n    ) dut (\n        .clk     (clk),\n        .reset_n (reset_n),\n        .enable  (enable),\n        .count   (count)\n    );\n\n    initial clk = 1&#x27;b0;\n\n    always #5 clk = ~clk;\n\n    initial begin\n        reset_n = 1&#x27;b0;\n        enable  = 1&#x27;b0;\n\n        #20;\n        reset_n = 1&#x27;b1;\n        enable  = 1&#x27;b1;\n\n        #50;\n        enable = 1&#x27;b0;\n\n        #20;\n        $finish;\n    end\n\nendmodule</code></pre>\n<h3>Verification</h3>\n<p>SystemVerilog supports progressive verification.</p>\n<h4>Introductory Verification</h4>\n<ul><li>Directed test cases</li><li>Output comparisons</li><li><code>$display</code></li><li><code>$error</code></li><li>Basic assertions</li><li>Waveform analysis</li></ul>\n<h4>Advanced Verification</h4>\n<ul><li>SystemVerilog Assertions</li><li>Functional coverage</li><li>Constrained randomization</li><li>Classes</li><li>UVM</li></ul>\n<p>Advanced features will be introduced only when required by the verification workflow.</p>\n<h3>AI-Generated Code Review</h3>\n<p>AI-generated SystemVerilog must be reviewed for:</p>\n<ul><li>Correct module interfaces</li><li>Correct signal widths</li><li>Correct signed or unsigned behavior</li><li>Correct blocking and nonblocking assignments</li><li>Complete combinational assignments</li><li>Correct clock edge</li><li>Correct reset polarity</li><li>Synthesizable constructs</li><li>Unsupported tool features</li><li>Verification completeness</li></ul>\n<p>Example of an incomplete combinational block:</p>\n<pre><code class=\"language-systemverilog\">always_comb begin\n    if (enable)\n        y = a;\nend</code></pre>\n<p>Problem:</p>\n<ul><li><code>y</code> is not assigned when <code>enable</code> is <code>0</code>.</li><li>The tool may infer storage behavior.</li><li>This conflicts with the intended combinational logic.</li></ul>\n<p>Corrected version:</p>\n<pre><code class=\"language-systemverilog\">always_comb begin\n    y = &#x27;0;\n\n    if (enable)\n        y = a;\nend</code></pre>\n<h3>Use with ATI and OSS</h3>\n<p>ATI is used to:</p>\n<ul><li>Generate RTL.</li><li>Generate testbenches.</li><li>Review code.</li><li>Debug simulation failures.</li><li>Improve engineering prompts.</li><li>Create automation scripts.</li></ul>\n<p>OSS is used to:</p>\n<ul><li>Compile SystemVerilog.</li><li>Simulate supported language constructs.</li><li>Generate waveforms.</li><li>Inspect functional behavior.</li><li>Synthesize supported RTL.</li><li>Produce implementation reports.</li></ul>\n<h3>Industry Job Relevance</h3>\n<p>SystemVerilog is commonly required for roles such as:</p>\n<ul><li>RTL Design Engineer</li><li>ASIC Design Engineer</li><li>FPGA Design Engineer</li><li>SoC Design Engineer</li><li>IP Design Engineer</li><li>Design Verification Engineer</li><li>Front-End Design Engineer</li></ul>\n<p>The expected skill is not limited to remembering syntax.</p>\n<p>Employers expect engineers to:</p>\n<ul><li>Translate specifications into RTL.</li><li>Review and debug RTL.</li><li>Build verification environments.</li><li>Analyze simulation failures.</li><li>Understand synthesis implications.</li><li>Communicate design intent.</li><li>Work with design and verification teams.</li></ul>"
    },
    {
      "id": "file-extensions",
      "title": "File Extensions",
      "topics": [],
      "contentHtml": "<h3><code>.v</code> — Verilog Source File</h3>\n<p>Use <code>.v</code> for source code intended to be compiled as Verilog.</p>\n<p>Example filename:</p>\n<pre><code class=\"language-text\">mux2.v</code></pre>\n<p>Typical contents:</p>\n<pre><code class=\"language-verilog\">module mux2 (\n    input  wire a,\n    input  wire b,\n    input  wire select,\n    output wire y\n);\n\n    assign y = select ? b : a;\n\nendmodule</code></pre>\n<p>Use <code>.v</code> when:</p>\n<ul><li>Maintaining an existing Verilog project.</li><li>The tool flow expects Verilog source files.</li><li>SystemVerilog constructs are not required.</li><li>Compatibility with an older tool is necessary.</li></ul>\n<h3><code>.sv</code> — SystemVerilog Source File</h3>\n<p>Use <code>.sv</code> for:</p>\n<ul><li>SystemVerilog RTL modules</li><li>SystemVerilog testbenches</li><li>Packages</li><li>Interfaces</li><li>Assertions</li><li>Class-based verification code</li></ul>\n<p>Example filename:</p>\n<pre><code class=\"language-text\">mux2.sv</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-systemverilog\">module mux2 (\n    input  logic a,\n    input  logic b,\n    input  logic select,\n    output logic y\n);\n\n    always_comb begin\n        y = select ? b : a;\n    end\n\nendmodule</code></pre>\n<p>Use <code>.sv</code> when the code contains constructs such as:</p>\n<pre><code class=\"language-systemverilog\">logic\nalways_comb\nalways_ff\nalways_latch\ntypedef\nenum\ninterface\nclass</code></pre>\n<h3><code>.svh</code> — SystemVerilog Header File</h3>\n<p>Use <code>.svh</code> for reusable content that is included in another SystemVerilog source file.</p>\n<p>Typical contents include:</p>\n<ul><li>Macro definitions</li><li>Shared declarations</li><li>Common constants</li><li>Include guards</li><li>Small reusable definitions</li></ul>\n<p>Example header:</p>\n<pre><code class=\"language-systemverilog\">`ifndef PROJECT_DEFINES_SVH\n`define PROJECT_DEFINES_SVH\n\n`define DATA_WIDTH 8\n`define ADDR_WIDTH 4\n\n`endif</code></pre>\n<p>Including the header:</p>\n<pre><code class=\"language-systemverilog\">`include &quot;project_defines.svh&quot;\n\nmodule register_block (\n    input  logic                   clk,\n    input  logic [`DATA_WIDTH-1:0] data_in,\n    output logic [`DATA_WIDTH-1:0] data_out\n);\n\n    always_ff @(posedge clk) begin\n        data_out &lt;= data_in;\n    end\n\nendmodule</code></pre>\n<h3><code>.svh</code> Is Not a Separate Language</h3>\n<ul><li><code>.svh</code> normally contains SystemVerilog source fragments.</li><li>It is inserted into another file using the `<code> </code>include `` directive.</li><li>It is generally not compiled as an independent design unit.</li><li>Incorrect include ordering can create:<ul><li>Duplicate definitions</li><li>Missing macros</li><li>Hidden dependencies</li><li>Compilation-order problems</li></ul></li></ul>\n<h3>Prefer Packages for Structured Reuse</h3>\n<p>For shared types and parameters, a package is often clearer than a large collection of header files.</p>\n<p>Example package:</p>\n<pre><code class=\"language-systemverilog\">package controller_pkg;\n\n    parameter int DATA_WIDTH = 8;\n\n    typedef enum logic [1:0] {\n        IDLE,\n        LOAD,\n        RUN,\n        DONE\n    } state_t;\n\nendpackage</code></pre>\n<p>Using the package:</p>\n<pre><code class=\"language-systemverilog\">module controller\n    import controller_pkg::*;\n(\n    input  logic clk,\n    input  logic reset_n,\n    output state_t current_state\n);\n\n    always_ff @(posedge clk or negedge reset_n) begin\n        if (!reset_n)\n            current_state &lt;= IDLE;\n        else\n            current_state &lt;= RUN;\n    end\n\nendmodule</code></pre>\n<p>Packages will be discussed when reusable SystemVerilog types are introduced.</p>\n<h3>File Extension Does Not Replace Tool Configuration</h3>\n<p>A source extension usually tells the tool which language parser to use, but tool behavior can vary.</p>\n<p>Always verify:</p>\n<ul><li>The file is compiled in SystemVerilog mode.</li><li>The selected simulator supports the constructs used.</li><li>The synthesis tool supports the synthesizable subset used.</li><li>Source files are compiled in the correct order.</li><li>Included files and packages are available.</li></ul>\n<h3>Typical Project Structure</h3>\n<pre><code class=\"language-text\">project/\n├── rtl/\n│   ├── counter.sv\n│   ├── controller.sv\n│   └── project_pkg.sv\n├── tb/\n│   └── counter_tb.sv\n├── include/\n│   └── project_defines.svh\n└── sim/\n    └── run_simulation</code></pre>\n<h3>Recommended Rule for This Course</h3>\n<ul><li>Use <code>.sv</code> for new RTL modules.</li><li>Use <code>.sv</code> for SystemVerilog testbenches.</li><li>Use <code>.svh</code> only for content that must be included.</li><li>Use packages for reusable types, parameters and enumerations.</li><li>Use <code>.v</code> only when working with Verilog-only source code or older tool requirements.</li></ul>\n<pre><code></code></pre>\n<p>The previous response lost the markdown structure because of formatting. Below is the correct continuation matching your <code>content.md</code> format.</p>\n<pre><code class=\"language-markdown\"></code></pre>"
    },
    {
      "id": "the-logic-data-type",
      "title": "The `logic` Data Type",
      "topics": [],
      "contentHtml": "<h3>Why Was <code>logic</code> Introduced?</h3>\n<p>Traditional Verilog uses two primary data types:</p>\n<ul><li><code>wire</code></li><li><code>reg</code></li></ul>\n<p>These names often confuse beginners because:</p>\n<ul><li><code>reg</code> does <strong>not</strong> necessarily represent a hardware register.</li><li><code>wire</code> does <strong>not</strong> necessarily represent a physical wire.</li></ul>\n<p>They describe <strong>how a signal is assigned</strong>, not the hardware that will be synthesized.</p>\n<p>SystemVerilog introduces the <strong><code>logic</code></strong> data type to simplify RTL design.</p>\n<p>For most synthesizable RTL designs, <code>logic</code> replaces both <code>wire</code> and <code>reg</code>.-</p>\n<h3>Verilog Example</h3>\n<pre><code class=\"language-verilog\">module mux2 (\n    input  wire a,\n    input  wire b,\n    input  wire select,\n    output reg  y\n);\n\nalways @(*) begin\n    if (select)\n        y = b;\n    else\n        y = a;\nend\n\nendmodule</code></pre>\n<p>Although <code>y</code> is declared as <code>reg</code>, the synthesized hardware is still a multiplexer.-</p>\n<h3>Equivalent SystemVerilog Code</h3>\n<pre><code class=\"language-systemverilog\">module mux2 (\n    input  logic a,\n    input  logic b,\n    input  logic select,\n    output logic y\n);\n\nalways_comb begin\n    if (select)\n        y = b;\n    else\n        y = a;\nend\n\nendmodule</code></pre>\n<p>Advantages:</p>\n<ul><li>Single signal type for most RTL.</li><li>Easier to understand.</li><li>Less coding confusion.</li><li>Better compiler diagnostics.-</li></ul>\n<h3>Driver Rules</h3>\n<p>A <code>logic</code> signal should normally have <strong>one driver</strong>.</p>\n<p>Valid examples:</p>\n<pre><code class=\"language-systemverilog\">logic y;\n\nassign y = a &amp; b;</code></pre>\n<p>or</p>\n<pre><code class=\"language-systemverilog\">logic y;\n\nalways_comb\n    y = a &amp; b;</code></pre>\n<p>Avoid multiple drivers.</p>\n<p>Incorrect example:</p>\n<pre><code class=\"language-systemverilog\">logic y;\n\nassign y = a;\nassign y = b;</code></pre>\n<p>Many tools will report this as an error.-</p>\n<h3>Best Practices</h3>\n<ul><li>Use <code>logic</code> for:<ul><li>Inputs</li><li>Outputs</li><li>Internal signals</li><li>Registers</li><li>FSM state variables</li></ul></li><li>Prefer <code>logic</code> over <code>wire</code> and <code>reg</code> in new designs.</li><li>Use one driver for every <code>logic</code> signal.-</li></ul>"
    },
    {
      "id": "always-comb",
      "title": "`always_comb`",
      "topics": [],
      "contentHtml": "<h3>Purpose</h3>\n<p><code>always_comb</code> is used to describe <strong>combinational logic</strong>.</p>\n<p>Typical applications include:</p>\n<ul><li>Multiplexers</li><li>Decoders</li><li>Encoders</li><li>Comparators</li><li>ALU combinational logic</li><li>Next-state logic of FSMs-</li></ul>\n<h3>Why Not Use <code>always @(*)</code>?</h3>\n<p>Traditional Verilog:</p>\n<pre><code class=\"language-verilog\">always @(*)</code></pre>\n<p>SystemVerilog:</p>\n<pre><code class=\"language-systemverilog\">always_comb</code></pre>\n<p>Benefits of <code>always_comb</code>:</p>\n<ul><li>Clearly indicates combinational intent.</li><li>Automatically creates the sensitivity list.</li><li>Enables additional compiler checks.</li><li>Improves code readability.-</li></ul>\n<h3>Example</h3>\n<pre><code class=\"language-systemverilog\">module mux2 (\n    input  logic a,\n    input  logic b,\n    input  logic select,\n    output logic y\n);\n\nalways_comb begin\n    if (select)\n        y = b;\n    else\n        y = a;\nend\n\nendmodule</code></pre>\n<p>This synthesizes into a <strong>2-to-1 multiplexer</strong>.-</p>\n<h3>Common Coding Mistake</h3>\n<pre><code class=\"language-systemverilog\">always_comb begin\n    if (enable)\n        y = a;\nend</code></pre>\n<p>Problem:</p>\n<ul><li><code>y</code> is not assigned when <code>enable</code> is <code>0</code>.</li><li>The combinational description is incomplete.-</li></ul>\n<h3>Correct Version</h3>\n<pre><code class=\"language-systemverilog\">always_comb begin\n    y = 1&#x27;b0;\n\n    if (enable)\n        y = a;\nend</code></pre>\n<p>Every execution path now assigns a value to <code>y</code>.-</p>\n<h3>Best Practices</h3>\n<ul><li>Assign every output in every execution path.</li><li>Use blocking assignment (<code>=</code>).</li><li>Do not infer storage.</li><li>Keep only combinational logic inside <code>always_comb</code>.-</li></ul>"
    },
    {
      "id": "always-ff",
      "title": "`always_ff`",
      "topics": [],
      "contentHtml": "<h3>Purpose</h3>\n<p><code>always_ff</code> is used to model <strong>edge-triggered sequential logic</strong>.</p>\n<p>Typical hardware includes:</p>\n<ul><li>Registers</li><li>Counters</li><li>Shift registers</li><li>Pipeline registers</li><li>FSM state registers-</li></ul>\n<h3>Single Clocking Event</h3>\n<p>Typical syntax:</p>\n<pre><code class=\"language-systemverilog\">always_ff @(posedge clk)</code></pre>\n<p>or</p>\n<pre><code class=\"language-systemverilog\">always_ff @(posedge clk or negedge reset_n)</code></pre>\n<p>The block represents flip-flop behavior.-</p>\n<h3>Example</h3>\n<pre><code class=\"language-systemverilog\">module dff (\n    input  logic clk,\n    input  logic d,\n    output logic q\n);\n\nalways_ff @(posedge clk)\n    q &lt;= d;\n\nendmodule</code></pre>\n<p>Synthesized hardware:</p>\n<ul><li>One positive-edge triggered D flip-flop.-</li></ul>\n<h3>Register with Reset</h3>\n<pre><code class=\"language-systemverilog\">always_ff @(posedge clk or negedge reset_n) begin\n\n    if (!reset_n)\n        q &lt;= 1&#x27;b0;\n    else\n        q &lt;= d;\n\nend\n```- \n\n### Why Use Nonblocking Assignment?\n\nSequential logic should use:\n\n```systemverilog\n&lt;=</code></pre>\n<p>instead of</p>\n<pre><code class=\"language-systemverilog\">=</code></pre>\n<p>This models flip-flop behavior correctly during simulation.-</p>\n<h3>Best Practices</h3>\n<ul><li>One clock domain per block.</li><li>Use nonblocking assignments.</li><li>Separate combinational and sequential logic.</li><li>Use <code>logic</code> for registers.-</li></ul>"
    },
    {
      "id": "always-latch",
      "title": "`always_latch`",
      "topics": [],
      "contentHtml": "<h3>Purpose</h3>\n<p><code>always_latch</code> is used to model <strong>level-sensitive latches</strong>.</p>\n<p>Unlike flip-flops, latches are transparent while their enable signal is active.-</p>\n<h3>Example</h3>\n<pre><code class=\"language-systemverilog\">always_latch begin\n\n    if (enable)\n        q &lt;= d;\n\nend</code></pre>\n<p>Synthesized hardware:</p>\n<ul><li>One level-sensitive latch.-</li></ul>\n<h3>Why Are Latches Usually Avoided?</h3>\n<p>Unintended latches can introduce:</p>\n<ul><li>Difficult timing analysis.</li><li>More complex verification.</li><li>Harder debugging.</li></ul>\n<p>Most commercial RTL is implemented using:</p>\n<ul><li>Combinational logic.</li><li>Flip-flops.-</li></ul>\n<h3>Appropriate Uses</h3>\n<p>Intentional latches are occasionally used in:</p>\n<ul><li>Clock gating.</li><li>Low-power circuits.</li><li>Technology-specific IP.-</li></ul>\n<h3>Best Practices</h3>\n<ul><li>Use <code>always_latch</code> only when a latch is intentionally required.</li><li>Never rely on incomplete <code>always_comb</code> blocks to infer latches.-</li></ul>"
    },
    {
      "id": "typedef",
      "title": "`typedef`",
      "topics": [],
      "contentHtml": "<h3>Purpose</h3>\n<p><code>typedef</code> allows engineers to create <strong>reusable data types</strong>.</p>\n<p>Instead of repeating long declarations, define the type once and reuse it.-</p>\n<h3>Without <code>typedef</code></h3>\n<pre><code class=\"language-systemverilog\">logic [7:0] data_a;\nlogic [7:0] data_b;\nlogic [7:0] data_c;\n```- \n\n### With `typedef`\n\n```systemverilog\ntypedef logic [7:0] byte_t;\n\nbyte_t data_a;\nbyte_t data_b;\nbyte_t data_c;</code></pre>\n<p>Advantages:</p>\n<ul><li>Cleaner code.</li><li>Better readability.</li><li>Easier maintenance.</li><li>Consistent signal widths.-</li></ul>\n<h3>Example with FSM States</h3>\n<pre><code class=\"language-systemverilog\">typedef enum logic [1:0] {\n\n    IDLE,\n    LOAD,\n    RUN,\n    DONE\n\n} state_t;</code></pre>\n<p>The new type <code>state_t</code> can now be used throughout the RTL.-</p>\n<h3>Best Practices</h3>\n<ul><li>Create reusable type names.</li><li>Use meaningful names.</li><li>Place common type definitions inside packages.</li><li>Combine <code>typedef</code> with <code>enum</code> for FSM design.</li></ul>\n<pre><code>\n````markdown id=&quot;v7t3nk&quot;</code></pre>"
    },
    {
      "id": "enum",
      "title": "`enum`",
      "topics": [],
      "contentHtml": "<h3>Why Use <code>enum</code>?</h3>\n<p>Finite State Machines (FSMs) are widely used in digital design.</p>\n<p>Examples include:</p>\n<ul><li>UART controllers</li><li>SPI controllers</li><li>Traffic light controllers</li><li>Memory controllers</li><li>Communication protocols</li></ul>\n<p>Traditionally, FSM states were defined using <code>parameter</code> or <code>localparam</code>.</p>\n<p>SystemVerilog introduces <strong>enumerated data types (<code>enum</code>)</strong> to make state definitions clearer and easier to maintain.-</p>\n<h3>Traditional Verilog FSM States</h3>\n<pre><code class=\"language-verilog\">parameter IDLE = 2&#x27;b00;\nparameter LOAD = 2&#x27;b01;\nparameter RUN  = 2&#x27;b10;\nparameter DONE = 2&#x27;b11;</code></pre>\n<p>Although functional, this approach:</p>\n<ul><li>Separates state declarations from the state variable.</li><li>Provides limited type checking.</li><li>Is less readable in large projects.-</li></ul>\n<h3>SystemVerilog Enumeration</h3>\n<pre><code class=\"language-systemverilog\">typedef enum logic [1:0] {\n\n    IDLE,\n    LOAD,\n    RUN,\n    DONE\n\n} state_t;</code></pre>\n<p>Advantages:</p>\n<ul><li>States are grouped together.</li><li>Easy to understand.</li><li>Stronger compiler checking.</li><li>Better readability.-</li></ul>\n<h3>Declaring the State Variable</h3>\n<pre><code class=\"language-systemverilog\">state_t current_state;\nstate_t next_state;</code></pre>\n<p>Instead of:</p>\n<pre><code class=\"language-systemverilog\">logic [1:0] current_state;\nlogic [1:0] next_state;</code></pre>\n<p>the state variable now clearly represents an FSM state.-</p>\n<h3>Example State Machine</h3>\n<pre><code class=\"language-systemverilog\">always_comb begin\n\n    case (current_state)\n\n        IDLE :\n            next_state = LOAD;\n\n        LOAD :\n            next_state = RUN;\n\n        RUN :\n            next_state = DONE;\n\n        DONE :\n            next_state = IDLE;\n\n    endcase\n\nend</code></pre>\n<p>The state names make the design much easier to understand than numeric values.-</p>\n<h3>Debugging Advantages</h3>\n<p>Most simulators display:</p>\n<pre><code class=\"language-text\">IDLE\nLOAD\nRUN\nDONE</code></pre>\n<p>instead of:</p>\n<pre><code class=\"language-text\">00\n01\n10\n11</code></pre>\n<p>Benefits:</p>\n<ul><li>Easier waveform analysis.</li><li>Faster debugging.</li><li>Easier review by other engineers.</li><li>Improved code documentation.-</li></ul>\n<h3>Best Practices</h3>\n<ul><li>Use <code>enum</code> for all FSM state definitions.</li><li>Combine <code>enum</code> with <code>typedef</code>.</li><li>Use meaningful state names.</li><li>Avoid manually assigning binary values unless required.-</li></ul>"
    },
    {
      "id": "combining-the-features",
      "title": "Combining the Features",
      "topics": [],
      "contentHtml": "<p>The features introduced so far are commonly used together in commercial RTL.</p>\n<p>A simple FSM demonstrates how they work together.-</p>\n<h3>Complete FSM Example</h3>\n<pre><code class=\"language-systemverilog\">module traffic_controller (\n\n    input  logic clk,\n    input  logic reset_n,\n    output logic green\n\n);\n\ntypedef enum logic [1:0] {\n\n    RED,\n    GREEN,\n    YELLOW\n\n} state_t;\n\nstate_t current_state;\nstate_t next_state;\n\nalways_ff @(posedge clk or negedge reset_n) begin\n\n    if (!reset_n)\n        current_state &lt;= RED;\n    else\n        current_state &lt;= next_state;\n\nend\n\nalways_comb begin\n\n    next_state = current_state;\n    green = 1&#x27;b0;\n\n    case (current_state)\n\n        RED :\n            next_state = GREEN;\n\n        GREEN : begin\n            green = 1&#x27;b1;\n            next_state = YELLOW;\n        end\n\n        YELLOW :\n            next_state = RED;\n\n    endcase\n\nend\n\nendmodule\n```- \n\n### Features Used\n\n| Feature | Purpose |\n|---------|---------|\n| `logic` | Signal declarations |\n| `typedef` | Create reusable state type |\n| `enum` | Named FSM states |\n| `always_ff` | State register |\n| `always_comb` | Next-state and output logic |- \n\n### Commercial RTL Flow\n\n```text\nSpecification\n        ↓\nState Diagram\n        ↓\ntypedef + enum\n        ↓\nState Register (always_ff)\n        ↓\nNext-State Logic (always_comb)\n        ↓\nSimulation\n        ↓\nWaveform Review\n        ↓\nSynthesis</code></pre>\n<p>This is the coding style that will be used throughout the remainder of this course.-</p>"
    },
    {
      "id": "other-systemverilog-features",
      "title": "Other SystemVerilog Features",
      "topics": [],
      "contentHtml": "<p>The features covered so far are sufficient to begin writing commercial RTL.</p>\n<p>SystemVerilog also includes many additional capabilities that support large-scale verification.</p>\n<p>These topics will be introduced only when they become necessary.-</p>\n<h3>Interfaces</h3>\n<p>Purpose:</p>\n<ul><li>Group related signals together.</li><li>Simplify module connections.</li><li>Reduce port list complexity.</li></ul>\n<p>Common applications:</p>\n<ul><li>APB</li><li>AHB</li><li>AXI</li><li>SPI</li><li>UART-</li></ul>\n<h3>Assertions (SVA)</h3>\n<p>Purpose:</p>\n<ul><li>Verify that a design behaves correctly during simulation.</li><li>Detect protocol violations automatically.</li></ul>\n<p>Example applications:</p>\n<ul><li>Handshake checking.</li><li>Reset verification.</li><li>Timing relationships.</li><li>FSM transition checking.-</li></ul>\n<h3>Classes</h3>\n<p>Purpose:</p>\n<ul><li>Support object-oriented verification.</li><li>Create reusable verification components.</li></ul>\n<p>Used in:</p>\n<ul><li>Testbench architecture.</li><li>Verification libraries.</li><li>UVM.</li></ul>\n<p>Classes are <strong>not synthesizable</strong>.-</p>\n<h3>Randomization</h3>\n<p>Purpose:</p>\n<ul><li>Automatically generate large numbers of test cases.</li></ul>\n<p>Benefits:</p>\n<ul><li>Better corner-case testing.</li><li>Improved functional coverage.</li><li>Reduced manual test writing.-</li></ul>\n<h3>Packages</h3>\n<p>Purpose:</p>\n<ul><li>Store reusable:<ul><li>Parameters</li><li>Types</li><li>Enumerations</li><li>Functions</li><li>Shared declarations</li></ul></li></ul>\n<p>Packages improve code organization in large projects.-</p>\n<h3>UVM (Universal Verification Methodology)</h3>\n<p>Purpose:</p>\n<ul><li>Standard verification methodology used in the semiconductor industry.</li></ul>\n<p>Provides:</p>\n<ul><li>Reusable verification components.</li><li>Standard testbench architecture.</li><li>Regression support.</li><li>Functional coverage integration.</li></ul>\n<p>UVM is primarily used by verification engineers.-</p>\n<h3>Why Are These Topics Deferred?</h3>\n<p>Learning them now would interrupt the RTL design workflow.</p>\n<p>Instead, they will be introduced naturally when they become necessary.</p>\n<p>This approach keeps the focus on engineering rather than memorizing language features.-</p>"
    },
    {
      "id": "when-will-we-learn-them",
      "title": "When Will We Learn Them?",
      "topics": [],
      "contentHtml": "<p>The remaining SystemVerilog features will appear throughout the course as the engineering workflow evolves.</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Feature</th><th>Course Topic</th></tr></thead><tbody><tr><td>Packages</td><td>Reusable RTL libraries</td></tr><tr><td>Interfaces</td><td>Bus protocols (UART, SPI, APB, AHB, AXI)</td></tr><tr><td>Assertions</td><td>Functional verification</td></tr><tr><td>Classes</td><td>Testbench architecture</td></tr><tr><td>Randomization</td><td>Verification methodology</td></tr><tr><td>UVM</td><td>Advanced verification flow</td></tr></tbody></table></div>\n<p>This progression mirrors the workflow followed in commercial semiconductor projects.-</p>"
    },
    {
      "id": "industry-best-practices",
      "title": "Industry Best Practices",
      "topics": [],
      "contentHtml": "<p>Modern commercial RTL teams generally follow these guidelines.-</p>\n<h3>Signal Declarations</h3>\n<ul><li>Prefer <code>logic</code> over <code>wire</code> and <code>reg</code>.</li><li>Give signals meaningful names.</li><li>Use one driver per signal.-</li></ul>\n<h3>Combinational Logic</h3>\n<ul><li>Use <code>always_comb</code>.</li><li>Assign outputs in every execution path.</li><li>Use blocking assignments (<code>=</code>).-</li></ul>\n<h3>Sequential Logic</h3>\n<ul><li>Use <code>always_ff</code>.</li><li>Use nonblocking assignments (<code>&lt;=</code>).</li><li>Keep one clock domain per sequential block.-</li></ul>\n<h3>Latches</h3>\n<ul><li>Avoid unintended latches.</li><li>Use <code>always_latch</code> only when a latch is intentionally required.-</li></ul>\n<h3>State Machines</h3>\n<ul><li>Use <code>typedef enum</code>.</li><li>Use descriptive state names.</li><li>Separate state registers from next-state logic.-</li></ul>\n<h3>Reusable Design</h3>\n<ul><li>Use <code>typedef</code> for reusable types.</li><li>Store common types inside packages.</li><li>Build reusable RTL modules instead of duplicated code.-</li></ul>\n<h3>AI-Assisted Development</h3>\n<p>When reviewing AI-generated RTL:</p>\n<ul><li>Check synthesizability.</li><li>Verify design intent.</li><li>Review reset behavior.</li><li>Review state transitions.</li><li>Verify coding style.</li><li>Simulate before synthesis.</li></ul>\n<p>AI accelerates development, but engineering validation remains essential.-</p>"
    },
    {
      "id": "lecture-summary",
      "title": "Lecture Summary",
      "topics": [],
      "contentHtml": "<p>In this lecture, you learned:</p>\n<ul><li>Why SystemVerilog was developed.</li><li>How it extends traditional Verilog.</li><li>The purpose of <code>.sv</code> and <code>.svh</code> files.</li><li>How <code>logic</code> simplifies signal declarations.</li><li>How <code>always_comb</code> models combinational logic.</li><li>How <code>always_ff</code> models sequential logic.</li><li>When <code>always_latch</code> should be used.</li><li>How <code>typedef</code> improves code reuse.</li><li>How <code>enum</code> makes FSMs easier to design and debug.</li></ul>\n<p>You also received a brief introduction to:</p>\n<ul><li>Interfaces</li><li>Assertions</li><li>Classes</li><li>Randomization</li><li>Packages</li><li>UVM</li></ul>\n<p>These advanced SystemVerilog features will be introduced only when they become necessary in the engineering workflow.</p>\n<p>From the next lecture onward, SystemVerilog will be used as the primary RTL language throughout the course.</p>"
    }
  ]
};
